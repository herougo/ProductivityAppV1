# copy-plan

Copy this plan to the ai/plans folder. Using the following naming convention for the plan.

- full format: `<index>-<feature>.md`
- `index` should be an integer N, where N is the Nth document added to the folder, represented with 3 digits (e.g. `002`, `231`)
- example plan file name: `001-db-schema.md`
